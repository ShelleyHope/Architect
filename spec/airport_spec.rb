# require "airport.rb"

# describe "Airport" do
  
#   let(:airport)   { Airport.new         }
#   let(:airport_1) { Airport.new(:plane) }
 

# it "has no planes" do
#   expect(airport).to_not have_planes
# end
  